# TEX / PDF 全文检查记录

检查日期：2026-09-13。页码均指当前 20 页 PDF 的页码。

检查对象为 `newswarm_cja_english.tex`、`cja.cls`、`newswarm_cja_english.pdf` 及正文实际调用的图片。本次没有修改这些文件。独立编译输出存放在 `/tmp/cja_full_audit/build/`。

## 检查结论

当前稿件能够正常编译，但存在公式越栏、模板占位文字、图文符号与参数不一致，以及图片标签过小等问题。不能将“编译成功”理解为“已无排版或文字错误”。

- 独立运行 `latexmk -pdf -outdir=/tmp/cja_full_audit/build -interaction=nonstopmode -halt-on-error newswarm_cja_english.tex` 成功。
- 临时重编译 PDF 与现有 PDF 的提取文本完全相同。
- 已逐页查看全部 20 页，并对部分原始图单独放大查看。
- 119 个有效 LaTeX 标签，无重复标签、无未定义交叉引用。
- 34 条参考文献，无未定义文献引用；其中一条未在正文引用。
- 正文 38 次有效图片调用，文件均存在。
- 无致命编译错误、缺字报告、竖向溢出或超高浮动体警告。
- 有 14 条横向溢出警告及一条导入 PDF 版本警告。
- 未发现 PDF 中的 `??` 引用占位、中文残留或文本替换乱码字符。
- CJA 首页品牌信息和审稿声明未恢复，Elsevier 页底标识按要求保留；命题标题和证明标记没有残留。

## 优先处理的问题

| 编号 | 位置 | 实际发现 | 处理边界 |
|---|---|---|---|
| 01 | PDF p.7，式 (68)；TEX 805–806 行，日志在 812 行报告 | 长分式超出栏宽约 21.6863 pt（约 7.6 mm），侵入栏间区域，与右栏 `to construct` 所在位置挤在一起；编号也落到下一行。 | 这是公式排版问题，应调整断行或展示空间，保持数学内容不变。 |
| 02 | PDF 所有偶数页；CLS 992、1338 行 | 页眉仍显示 `Short Author names`。 | 明确的模板占位残留，应清除或填写实际作者短名。 |
| 03 | PDF p.10，实验设置说明；TEX 1246 行 | 实际输出为 `Appendix Appendix A`，第一个单词因换行显示为 `Ap-` / `pendix`。 | `\ref{app:complete-parameters}` 已返回带 Appendix 的字符串，正文又手工加了 Appendix。仅需修引用文字。 |
| 04 | PDF pp.10、11、13、15、19；TEX 1294、1397、1587、1814 行 | 表 2 采用 2.5g，图 6、10、14 的限制线也标 2.5g；6-vs-6、10-vs-10 文字和附录表 A.5 仍写 3g。 | 确定存在图文参数不一致，但检查本身不能决定哪一个才是实际值。须按对应实验记录统一。附录的归一化常数 3g 是另一概念，不应一并机械替换。 |
| 05 | PDF p.9，图 2；TEX 995、1030–1031 行 | 图 2 的 Decentralized Actor 中写 `Attention-Residual Block`，正文则将 actor 描述为 residual MLP，并明确 cross-agent attention 仅在 centralized critic 中使用。 | 图与正文的架构描述不一致，应核对当前采用的版本，再同步图文；本次未判断或更改算法。 |
| 06 | PDF pp.12、14、16，图 8a、12a、16a；TEX 1437、1531、1625 行 | 三张任务概率热图使用带帽的 pi 符号，而正文使用 mu 表示任务概率，pi 用于策略。 | 确定存在符号命名差异。应明确对应关系或统一图中标记。 |
| 07 | PDF pp.11、13、15，图 5、9、13 | 三维轨迹图中可见高度刻度，但 z 轴名称和单位在成稿中不可见；单独查看 6-vs-6 源 PDF 也未见该轴标题。 | 应核对源图导出边界及轴标签，不能只靠增加 LaTeX 显示宽度解决。未据此断言轨迹数据有误。 |
| 08 | PDF p.1，摘要首句；TEX 50 行 | `..., therefore, ...` 用逗号连接两个独立分句，属于 comma splice。 | 改句号或分号即可，无需修改摘要主张。 |

## 文字、引用和说明完整性

| 编号 | 位置 | 实际发现 | 建议 |
|---|---|---|---|
| 09 | PDF p.3，式 (4) 后；TEX 141、144 行 | 公式末尾用句号，但下一行以小写 `and the closing velocity...` 接续。 | 统一为同一句的标点，或将后文改成独立句。 |
| 10 | PDF p.3，式 (17) 后；TEX 268–271 行 | 公式以逗号结束，下一句却大写 `Its magnitude is...`。 | 统一公式末尾与下一句的标点关系。 |
| 11 | PDF p.18，参考文献 34；TEX 1787–1788 行 | `WeiYang2022CJA` 有 bibitem，但有效正文没有引用。 | 确认是否误留。不应仅为保留条目而随意补引用。 |
| 12 | PDF pp.2、6、8 | HVT 没有在有效正文首次出现时展开；CTDE 的英文全称在引言出现，但没有与缩写正式对应；MLP 直接使用缩写。 | 统一首次出现的全称与缩写。无需对后文逐次重复展开。 |
| 13 | PDF p.9，图 2 内文字 | 存在 `Store storing` 重复措辞；critic 旁的 `V_phi=(s,zeta)` 与图中另一处函数记法不一致；图中还使用 `p_lock`、`Phi_game` 等与正文不同的标记。 | 图内文字也需要与正文一起校对。先确认指代相同，再统一标签，勿机械替换不同变量。 |
| 14 | PDF p.10，图 3 | 箭头名使用动词 `Observe`，而另一方向使用名词 `Action`；`Five attackers Online inference` 大小写和语序不自然。 | 属于可直接修正的图内英文问题。 |
| 15 | PDF pp.19–20，附录 B；TEX 1898 行及三个初始状态表 | 声称列出 complete per-UAV initial states，但表格只有位置、速度和航向，未列前文状态中的航迹倾角 gamma。 | 补实际参数或说明固定值/省略范围，不能编造缺失数值。 |

## 图片清晰度和版式

| 编号 | 位置 | 实际发现 | 判断 |
|---|---|---|---|
| 16 | PDF pp.11、13、15，图 5、9、13 | 三维轨迹图图例显著小于正文。提取的字体尺寸估算中，`HVT/start/terminal/direction` 等约为 3–4 pt。 | 源图以矢量为主，问题是缩印后太小，而非缺少像素。优先考虑源图文字大小和图例组织。 |
| 17 | PDF p.9 图 2；pp.12、14、16 任务热图 | 图 2 部分说明约 4.7–5.3 pt，热图行标签、颜色条及上下标也偏小。 | 放大可读不等于纸面可读。应在最终版面尺寸检查。字体估算来自 PDF 文本解析，不是期刊标准判定。 |
| 18 | PDF pp.11、13、15，距离子图；pp.12、14、16，威胁曲线 | 图例放在曲线区域，遮住部分线条；距离图的 `500m` 灰色标记与图例重叠。 | 不属于图片丢失，但影响读图；可以调整图例位置、留白或显示范围，数据保持原样。 |
| 19 | PDF p.1 | 标题与摘要之间有较大空白。作者、单位被注释后，模板仍保留标题/作者/日期区固定间距。 | 是模板留白，不是正文缺失。CLS 1061、1072、1088 行附近的固定间距参与形成空白。 |
| 20 | PDF p.20；TEX 1960 行 | 最后一页只有左上方一张表，其余大面积留白；源文件在该表之前显式 `\clearpage`。 | 属于可改善的版式，不是编译错误。第 19 页已较满，不能保证单纯删除换页就会减少总页数。 |
| 21 | PDF p.4，式 (18)、(30)、(33) | 公式末端与右侧编号非常接近，尤其矩阵后的式 (18) 和式 (30)。 | 即使没有相应的独立 overfull 警告，也值得调整公式与编号之间的空间。 |
| 22 | PDF pp.12、14、16；TEX 1436–1446、1530–1540、1624–1634 行 | 三组任务图各有 9 pt 横向溢出；子图使用 `0.50\textwidth`/`0.48\textwidth`，外层却是单栏 figure，导致尺寸相对栏宽不完全匹配。 | 当前未见整图相互覆盖，但边界对齐不一致。属于显示尺寸问题。 |
| 23 | PDF p.1 页底 Elsevier 框 | 模板首页产生 8.68678 pt 横向溢出；当前可见框体完整，没有遮住右栏正文。 | 轻微版式问题，保留用户要求的 Elsevier 标识即可；如做排版整理再微调框宽。 |

图片分辨率核查：图 1 内嵌位图约 1358 ppi，HIL 平台图约 516 ppi，热图位图约 409 ppi。没有发现明显由低像素造成的模糊；高像素并不能解决文字缩印过小。其余主要曲线图及框图包含矢量文字和路径。

## 需要确认，不能直接判为错误的项目

1. 图 4 的训练横轴到 2000 episodes，表 A.7 列出的训练长度为 3000 episodes。可能只展示前 2000 回合，也可能是版本差异；需明确展示范围，不能仅凭横轴认定训练数据错误。
2. 图 4 有阴影带，但正文和图注没有说明阴影是标准差、置信区间还是其他统计量，也没有明确对应的重复次数。
3. 首页没有作者和单位，PDF Author 元数据为空。若是匿名评审稿，这可能符合作者意图；若是具名稿，则信息不完整。两种情况都不应留下 `Short Author names`。
4. 附录表从 A.5–A.7、B.8–B.10 编号，而不是每个附录重新从 1 开始。这来自模板连续计数，现有引用是有效的；是否改为 A.1/B.1 应由目标期刊格式决定。

## 编译警告明细

| TEX 行号 | 横向超出量（pt） | 对应内容 |
|---|---:|---|
| 65 | 8.68678 | maketitle / 首页模板 |
| 91–92 | 0.878 | 贡献段落 |
| 95 | 0.815 | 第二节标题 |
| 364–365 | 11.28012 | 联合指标公式后的说明 |
| 433–434 | 6.70207 | 自风险公式后的说明 |
| 812 | 21.6863 | align 环境，主要问题为式 (68) |
| 979–980 | 5.8488 | CTDE 说明中的行内全局状态 |
| 1343–1344 | 7.77792 | 指标定义前的长句 |
| 1436–1446 | 9.0 | 第一组任务图 |
| 1526–1527 | 11.41988 | 案例文字 |
| 1530–1540 | 9.0 | 第二组任务图 |
| 1624–1634 | 9.0 | 第三组任务图 |
| 1676–1677 | 3.13116 | 消融文字 |
| 1676–1677 | 6.45871 | 同一段另一行 |

另有 `algorithm_framework.pdf` 为 PDF 1.7，而当前输出目标为 PDF 1.5 的兼容性警告。当前页面渲染正常，不应将该警告描述成图片已经损坏。

`chktex` 的静态提示经过人工筛选，没有直接按提示数量统计错误。例如区间 `(-pi,pi]` 的括号不同是合法数学记法；双连字符、数学括号等多条提示属于误报或样式偏好。

## 本次边界及修改顺序

本次是语言、图文一致性、引用、编译与显示审查，不是数学正确性证明、实验数据真实性验证或参考文献逐条外部核验。参数和图文矛盾只标明需要核实，不代替作者决定真实实现。

建议先处理 01–08，再处理文字与图内标记，最后整理字体大小、图例和页面留白。未改动原论文、模板或当前 PDF。

检查时文件 SHA-256：

```text
newswarm_cja_english.tex  3d848c5f0c81953b2110d51665a874c4d2c223b47428b3714321ef8cd35900d7
cja.cls                 1c57fb1acbfb0a219578a70342731fe470d911f23690573edb9577c2930c220f
newswarm_cja_english.pdf  6925a658385f003c2a6b6f4273d8c79afa15230b8e40bc4aeab4149b554cc0de
```
